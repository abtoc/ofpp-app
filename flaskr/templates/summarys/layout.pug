extends layout.pug

block menu
    //li: a(href="{{ url_for('performlogs.index',id=id,yymm=yymm) }}") 一覧
    //li: a(href="{{ url_for('performlogs.report',id=id,yymm=yymm) }}" target="_blank") 印刷
    //li: a(href="{{ url_for('performlogs.update',id=id,yymm=yymm) }}") 更新
