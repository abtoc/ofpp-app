include _form.pug
extends persons/layout.pug

block title
    | 利用者編集
block content
    form(method="POST")
        if id
            .form-group
                label ID
                .form-control= id
        +generate_fields(form)
        .btn-group
            input.btn.btn-default(type="submit" value="保存")
            a.btn.btn-default(href="{{ url_for('persons.index') }}") キャンセル
