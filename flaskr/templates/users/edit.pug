include _form.pug
extends timerules/layout.pug

block title
    | ユーザ登録
block content
    form(method="POST")
        +generate_fields(form)
        .btn-group
            input.btn.btn-success(type="submit" value="保存")
            a.btn.btn-danger(href="{{ url_for('users.index') }}") キャンセル
