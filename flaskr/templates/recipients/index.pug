extends persons/layout.pug

block title
  | 受給者証一覧 - ペタペタシステム
block content
    table.table.table-hover
        thead
            tr
                th
                th 名前
                th 受給者証番号
                th 利用開始日
                th 支給期間
                th 適用期間
                th 作成日
                th 更新日
                th
        tbody
            for item in items
                tr
                    td= '○' if item.person.enabled else ''
                    td= item.person.display_or_name
                    td= item.number if item.number else ''
                    td= item.usestart if item.usestart else ''
                    td= item.supply_out if item.supply_out else ''
                    td= item.apply_out if item.apply_out else ''
                    td= item.create_at | strftime('%Y/%m/%d %H:%M')
                    td= item.update_at | strftime('%Y/%m/%d %H:%M') 
                    td: .text-right: .btn-group
                        a.btn-sm.btn-primary(href="{{ url_for('recipients.edit', id=item.person_id) }}") 編集
