include _form.pug
extends timerules/layout.pug

block title
    | 各種設定
block content
    form(method="POST")
        +generate_fields(form)
        .btn-group
            input.btn.btn-default(type="submit" value="保存")
            a.btn.btn-default(href="{{ url_for('index') }}") キャンセル
